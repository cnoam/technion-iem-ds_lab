
def Model():
    """

    :param dataset_folder: all the dataset files are in this folder
    :return: list(tuple(string))
    """
    def __init__():
        pass

    def fit(x_train,y_train):
        pass

    def predict(x_test):
        return [('acq',), ('acq',), ('de',),('uk','de')]
    # in this sample output, there are 4 articles, each has 0 or more labels.
    # the order of the labels in the tuple does not matter.
    # the labels must be from the set defined in the dataset.

